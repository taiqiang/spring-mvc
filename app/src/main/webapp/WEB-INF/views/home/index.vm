$page.setVariable("title", "页面标题")

#block("title")
    默认标题
#end

<input value="$page.getToken()" type="hidden" />
<input value="$page.getTokenName()" type="hidden" />
<input value="$page.getTokenHeader()" type="hidden" />

$page.getGuid()

<script>
$page.renderTpl("encrypt", {
    "var": "encrypt",
    "keyToken": "01g3456789abcdef"
})
</script>

<div class="main">
    $page.renderTpl("go-search")
    $page.renderTpl("output.html")
</div>

#set($textMap = ["中文", "1234567890", "Alibaba-inc", "&"])
#foreach($raw_item in $textMap)
$page.encrypt($raw_item, "01g3456789abcdef")<br><script>document.write(encrypt("$raw_item"))</script><br><br>
#end

$page.setLayout("common/default")